FROM node:20-alpine

WORKDIR /app

ENV NODE_ENV=production

COPY package*.json ./
RUN npm ci --omit=dev

COPY . .

# Fail early with a clear error if the backend source tree is incomplete.
RUN test -d scripts
RUN test -d src

RUN mkdir -p uploads

EXPOSE 5000

CMD ["node", "scripts/start-api.js"]
