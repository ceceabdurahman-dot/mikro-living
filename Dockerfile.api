FROM node:20-alpine

WORKDIR /app

ENV NODE_ENV=production

COPY package*.json ./
RUN npm ci --omit=dev

COPY scripts ./scripts
COPY src ./src

RUN mkdir -p uploads

EXPOSE 5000

CMD ["node", "scripts/start-api.js"]
